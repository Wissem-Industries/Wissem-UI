when:
  - event: [push, pull_request]

steps:
  - name: install
    image: oven/bun:1.4.2-alpine
    commands:
      - bun install --frozen-lockfile

  - name: check
    image: oven/bun:1.4.2-alpine
    commands:
      - bun run check
